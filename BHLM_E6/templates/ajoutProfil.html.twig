{% extends 'base.html.twig' %}

{% block title %}Ajout{% endblock %}

{% block body %}
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-body card-formulaire">
          <h2 class="mb-4 text-center">Ajouter un type de profil recherché</h2>

          {{ form_start(ajoutProfilForm) }}

          <div class="row">
            <div class="col-md-6">
              {{ form_row(ajoutProfilForm.Profil, {
                attr: {'class': 'form-select'},
                label_attr: {'class': 'form-label'}
              }) }}
            </div>
            <div class="col-md-6">
              {{ form_row(ajoutProfilForm.Entreprise, {
                attr: {'class': 'form-select'},
                label_attr: {'class': 'form-label'}
              }) }}
            </div>
          </div>

          <div class="d-flex mt-3 row-submit">
            {{ form_row(ajoutProfilForm.cancel, {
              attr: {'class': 'btn btn-icone-suppr btn-primary'}
            }) }}
            {{ form_row(ajoutProfilForm.submit, {
              attr: {'class': 'btn btn-icone btn-primary'}
            }) }}
          </div>

          {{ form_end(ajoutProfilForm) }}

          {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-3">{{ message }}</div>
          {% endfor %}

        </div>
      </div>
    </div>
  </div>
{% endblock %}
